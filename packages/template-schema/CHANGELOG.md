# @proxima-io/template-schema

## 0.2.1

### Patch Changes

- `websiteDeployScaffoldSectionSchema` and shell schema accept canonical `values` (legacy `default_values` still supported). Fixes silent strip of scaffold content on `proxima deploy`.

## 0.2.0

### Minor Changes

- Add BuilderEmptyState export, LocalBusiness JSON-LD, data_mode field, buyer auth flows, catalog and cart improvements, templateizer CLI fixes
