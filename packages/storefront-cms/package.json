{
  "name": "@proxima-io/storefront-cms",
  "version": "0.1.0",
  "type": "module",
  "description": "CMS composition utilities for Proxima storefronts \u2014 normalization, tenant resolution, preview detection, and API client factory.",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist",
    "publish:pkg": "pnpm publish --no-git-checks"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  },
  "dependencies": {
    "@proxima-io/storefront-core": "workspace:*"
  },
  "publishConfig": {
    "access": "public"
  }
}
